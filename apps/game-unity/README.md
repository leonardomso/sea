# Unity client

The Unity project will be created here in Phase 3 with Unity 6.3 LTS, URP, an orthographic 2.5D setup, WebGL support, and macOS support.

The Unity Editor runs on the host machine. The local backend and admin services run through Docker Compose.
